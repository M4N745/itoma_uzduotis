import style from "./../assets/css/index.module.css"

export default function Index(){
    return (
        <main className={`${style.index} d-flex`}>
            <span className={`my-auto mx-auto h1`}>content</span>
        </main>
    );
}